The hero AI search — Clara's signature "ask me what to drink" pill. Prominent, never buried.

```jsx
<SearchBar
  placeholder="Ask Clara what to drink…"
  buttonLabel="Dive in"
  suggestions={['Something pink & frozen', 'Low-ABV spritz', "What's on the patio?"]}
  onSubmit={(q) => runSearch(q)}
/>
```
Rose submit button, sparkle AI affordance, teal focus ring. Pass `size="md"` for inline/secondary placements.
