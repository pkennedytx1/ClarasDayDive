Multi-line text field for message bodies (event-coordinator contact form).

```jsx
<Textarea label="Tell us about your event" rows={5} placeholder="Date, headcount, vibe…" />
```

Shares the Input shell — `label`, `hint`, `error`, and `rows`. Vertically resizable.
