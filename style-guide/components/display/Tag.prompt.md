Toggleable filter chip for drink families and food-truck categories.

```jsx
<Tag active>All</Tag>
<Tag onClick={() => filter('spritz')}>Spritz</Tag>
```
`active` fills ink; idle is a hairline outline that warms on hover.
